<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.siyes.training.comprehensive.mapper.OrderDetailMapper">
    <resultMap id="orderDetailResultMap" type="OrderDetail">
        <id property="id" column="order_id"/>
        <result property="buyerId" column="buyer_id" />
        <result property="totalAmount" column="total_amount" />
        <result property="status" column="status" />
        <result property="createdAt" column="created_at" />
        <result property="cancelledAt" column="cancelled_at" />

        <collection property="orderItems" ofType="OrderItem">
            <id property="id" column="oi_id" />
            <result property="orderId" column="oi_order_id" />
            <result property="productId" column="product_id" />
            <result property="quantity" column="quantity" />
            <result property="unitPrice" column="unit_price" />
        </collection>

        <collection property="products" ofType="Product">
            <id property="id" column="p_id" />
            <result property="name" column="product_name" />
            <result property="price" column="product_price" />
        </collection>
    </resultMap>
    <select id="findDetailById" resultMap="orderDetailResultMap">
        SELECT
            o.id AS order_id,
            o.buyer_id,
            o.total_amount,
            o.status,
            o.created_at,
            o.cancelled_at,
            oi.id AS oi_id,
            oi.order_id AS oi_order_id,
            oi.product_id,
            oi.quantity,
            oi.unit_price,
            p.id AS p_id,
            p.name AS product_name,
            p.price AS product_price
        FROM
            `orders` o
        LEFT JOIN
            `order_items` oi ON o.id = oi.order_id
        LEFT JOIN
            `products` p ON p.id = oi.product_id
        WHERE
            o.id = #{orderId}
    </select>
</mapper>